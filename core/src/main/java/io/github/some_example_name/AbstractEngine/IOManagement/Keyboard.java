package io.github.some_example_name.AbstractEngine.IOManagement;

// Placeholder for keyboard-specific polling logic — currently empty because IOManager queries
// LibGDX directly via Gdx.input.isKeyPressed; expand this if per-frame keyboard state caching is needed
public class Keyboard {
    public void update() {
        // Reserved for future keyboard pre-processing (e.g. buffered input, combo detection)
    }
}
